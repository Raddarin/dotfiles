[package]
name = "compress"
version = "0.1.0"
edition = "2021" # Mer stabil än 2024 just nu

[dependencies]
hyprland = { git = "https://github.com/hyprland-community/hyprland-rs" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
